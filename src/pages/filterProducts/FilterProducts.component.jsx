import styled from "styled-components/macro"

const FilterProducts = () => {
	//sort by: numberOfAsks/salesThisPeriod, popularity, deadstockSold
	//lastSale, lowestAsk, highestBid,
	//release date(must modify it)

	//filter by: category, price ranges,
	return null
}

export default FilterProducts
